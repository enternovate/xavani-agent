# Xavani Agent 0.2.0 Plan — "The Big Bang"

500 updates in 10 workstreams. Approved by Andile, 2026-08-22.
Targets: median task wall time < 90 s, lowest cost per successful task, <= 3 parallel children.
Gates per phase: ruff clean, targeted pytest, faux bench, real bench weekly.

2026-08-22 Phase 2 (W4) audit COMPLETE, commit afa800b. Findings:
- Built today: model roles resolve_role/resolve_role_model + tests +
  model.roles config schema keys (items 181-182).
- Already present in 0.1.x (audited, no rebuild): fallback chains
  (_fallback_chain/_try_activate_fallback), per-turn primary restore
  (cooldown), parallel tool executor, cache-hit telemetry in
  conversation_loop, SessionBudgetGovernor, /cost with per-turn meter,
  session DB per-call cost persistence, context compressor, file_state
  per-path locks. Items 183-199 audited-covered; no duplication built.
Gates at close: ruff clean; router suites 15 passed; validate_cmd 18
passed; faux bench 6/6 median_wall_s=0.1657.

## Progress log

2026-08-22 Session 2 (continued). Advisor reviewer role SHIPPED (items
252-255, commit cb528b3): xavani_cli/advisor.py review_turn via
call_llm on model_router advisor role; maybe_review hook in
AIAgent.chat appends [high]/[medium]/[low] notes inline; /advisor
status|enable|disable handler in cli.py; advisor excluded from gateway
slash menus (Slack 50 cap — parity test caught it, fixed via
_GATEWAY_EXCLUDED_COMMANDS per standing rule). Tests:
tests/xavani_cli/test_advisor.py (12). Gates: ruff clean; 338/338
run_agent suite; 143/143 registry parity; faux bench pass.

2026-08-22 Session 2. Gateway parity for /loop verified green (143/143
integration-tier registry tests; no exclusion needed). Watchdog loops
SHIPPED (items 47-48, commit 87e1b32): xavani_cli/loop_watchdog.py tick
runs one headless pass via xavani -z; /loop watch [every S] [passes N]
[budget USD] [alert C] <prompt> creates spec + no-agent cron job; job
silent while running, alerts on finish, removes itself; tick finalizes
on stop conditions + runaway detection. loop_runner.save() public
wrapper added. Tests: tests/xavani_cli/test_loop_watchdog.py (10).
Gates: ruff clean; 33/33 loop+watchdog; 143/143 registry parity; faux
bench 20/20 median_wall_s=0.1233.

2026-08-22 Phase 1 started. Audit found tools/approval.py (1,814 lines) already
covers W1 items 1-9, 14-18 partially or fully (risk tiers, allowlists, modes,
timeout, gateway parity, audit log). Revised W1 gap list: write journal,
/revert command, /permissions manager command, batch preview, dry-run.
Done this session: tools/write_journal.py + hook in _handle_write_file +
tests/tools/test_write_journal.py. Verified: ruff clean, 7/7 unit tests,
end-to-end rollback through the real handler, faux bench 6/6 pass
(median_wall_s=0.1478).


## W1 — Pre-flight approval gate (items 1-30)

1. Add permission mode key to cli-config.yaml schema: gated | session-trust | unrestricted.
2. Create xavani_cli/permissions.py: allowlist store, pattern matcher, audit log writer.
3. Allowlist persisted at ~/.xavani/permissions.json.
4. Pattern syntax: exact command, prefix wildcard, tool name.
5. Approval prompt renders before any non-read tool call in gated mode.
6. Prompt answers: once / allowed-for-session / always-for-pattern.
7. Session allowlist lives in memory only; never persisted.
8. Read-only tools bypass the gate: read, search, list, get_state.
9. Gate hooks into the tool dispatch path in run_agent.py so subagents inherit it.
10. Batch preview: print full action list; one approval covers the batch.
11. Dry-run mode: list actions, execute nothing.
12. Undo journal: every approved write records an inverse patch.
13. /undo slash command rolls back the last journaled write.
14. /permissions slash command: view, add, remove patterns.
15. Audit log at ~/.xavani/permissions.log with timestamps and decisions.
16. Gateway parity: approval prompts surface on messaging platforms.
17. Timeout default on prompts: deny after 300 s of silence.
18. Unrestricted mode prints a one-line warning at session start.
19. Tests: allowlist add/remove/match.
20. Tests: gate blocks write tool in gated mode.
21. Tests: session-allow survives within session, dies on restart.
22. Tests: always-allow persists and reloads.
23. Tests: subagent inherits parent mode.
24. Tests: batch approval covers all listed actions.
25. Tests: dry-run executes nothing.
26. Tests: undo journal inverse patch correctness.
27. Tests: audit log lines well-formed.
28. Tests: prompt timeout denies.
29. Docs: permission modes section in README.
30. Bench check: gate adds < 50 ms per approved call.

## W2 — Loop engine (items 31-100)

31. /loop slash command: rerun a task every N minutes.
32. /loop until <predicate>: stop on success predicate.
33. Stop condition: max passes.
34. Stop condition: token budget cap.
35. Stop condition: wall-clock limit.
36. Stop condition: cost cap in USD.
37. Loop state persisted to ~/.xavani/loops/<id>.json.
38. Resume loop after crash or restart.
39. /loops slash command: list active loops.
40. /loop stop <id>: cancel a loop.
41. Failure notes carried between passes (reflexion pattern).
42. Failure note compaction: cap notes at 10 most recent.
43. Best-of-N sampling: N attempts, judge prompt picks the winner.
44. Judge prompt template stored in skills, user-overridable.
45. Escalation ladder: smol model first, main model on fail.
46. Escalation ladder configurable per loop.
47. Watchdog loops via cron/jobs.py scheduler.
48. Watchdog alert channel: session, gateway, or both.
49. Nested loop guard: max depth 2.
50. Runaway detection: abort when passes produce identical output 3 times.
51. Per-pass telemetry: duration, tokens, cost, verdict.
52. Loop summary card at end: passes, best pass, total cost.
53. Loop output written to ~/.xavani/loops/<id>/pass-<n>.md.
54. Eval loop mode: run task set, score, iterate on failures.
55. Eval loop stops when score threshold reached.
56. Eval loop emits diff of strategy between passes.
57. Concurrency cap inside loops: 1 pass at a time (serial directive).
58. Loop prompt template: {task} {last_output} {failure_notes}.
59-100. Tests: each stop condition; resume; telemetry; judge tie-break; escalation; watchdog fire; runaway abort; docs page; bench of loop overhead.

## W3 — Eval loops and harness expansion (items 101-180)

101. Grow baseline_tasks.json from 6 to 20 tasks: coding category.
102. Add 10 data-extraction tasks.
103. Add 5 summarization tasks.
104. Add 5 planning tasks.
105. Add 5 file-manipulation tasks.
106. Add 5 workflow/business tasks.
107. Verifier type: jsonschema:.
108. Verifier type: pytest:.
109. Verifier type: diff:.
110. Verifier type: exit_code:.
111. Verifier type: llm_judge: with rubric file.
112. Verifier timeout per task.
113. /eval slash command: run suite in-session.
114. /eval --category: filter by category.
115. Scorecard output: pass count, median, p90, cost-per-success.
116. /eval-loop command: run, score, patch, rerun.
117. Eval loop max passes flag.
118. Eval loop score threshold flag.
119. CI regression gate: fail if median worsens > 10%.
120. CI regression gate: fail if cost-per-success worsens > 10%.
121. Results stored at scripts/task_bench/results/ with config fingerprint.
122. Config leaderboard: rank stored configs by cost-per-success.
123. Flake detection: run suite twice; drop tasks unstable across runs.
124. p95 wall time tracked alongside p90.
125. Per-category medians in scorecard.
126. --tasks flag: run a custom task file.
127. --model flag: pin bench model.
128. Faux mode covers every new verifier type.
129. Task authoring guide in scripts/task_bench/README.md.
130. Verifier unit tests for each type.
131-180. One test item per new task (50 items): each new task has a faux-mode test asserting verifier passes on scripted output.

## W4 — Harness speed and cost hardening (items 181-240)

181. Model roles: default, smol, slow, plan, advisor in model_router.py.
182. Role config keys in cli-config.yaml.
183. Fallback chain per role on 429 or quota errors.
184. Cooldown restore to primary after success window.
185. Stable system-prompt prefix ordering for prompt-cache hits.
186. Cache-hit telemetry: report cached vs fresh tokens per turn.
187. Parallel execution of independent tool calls in one assistant turn.
188. Dependency guard: calls sharing a file stay serial.
189. Context pruning: drop tool outputs older than N turns past a size cap.
190. Pruning preserves the most recent 3 tool outputs always.
191. Early-exit verifier: score partial output when the task allows.
192. Token budget advisor: warn before a turn exceeds budget.
193. Per-task cost ledger written next to transcripts.
194. /cost slash command: session cost so far.
195. /cost --task: cost of last task.
196. Smol-model default for subagent fan-out.
197. Plan role used by plan mode.
198. Bench each change against 0.1.2 baseline.
199. Reject a change that worsens median or cost-per-success.
200-240. Tests per mechanism (cache telemetry, parallel guard, pruning caps, fallback, roles) plus docs and bench records.

## W5 — Ports from oh-my-pi, implemented better (items 241-330)

241. Hash-anchored edit format: anchor lines by content hash.
242. Stale-anchor rejection before any write.
243. Auto-recovery: suggest re-anchored patch on stale hit.
244. Token savings telemetry for anchored edits.
245. checkpoint tool: mark conversation state.
246. rewind tool: prune exploratory context, keep report.
247. retain tool: queue durable facts to xavani_memory/.
248. recall tool: search memory bank.
249. reflect tool: synthesize over the bank.
250. learn tool: capture reusable lesson; promote to skill.
251. memory_edit tool: update or invalidate by id.
252. Advisor reviewer role: second model reads each turn.
253. Advisor notes injected inline with severity.
254. Advisor runs on its own context and model.
255. /advisor slash command: status, enable, disable.
256. Agent hub roster: live subagent list with cost and duration.
257. Hub steer: send a message to a running child.
258. Hub kill: stop one child without aborting parent.
259. Hub revive: restart a parked child.
260. Magic keyword ultrathink: raise reasoning effort.
261. Magic keyword orchestrate: parallel subagent fan-out.
262. Magic keyword workflowz: deterministic multi-subagent workflow.
263. Keyword matching only in prose, never in code spans.
264. /fresh command: reset provider stream state, keep transcript.
265. Director mode: drive fast/good worker sessions with read-only toolset.
266. Atomic commit splitter: group working tree into ordered commits.
267. Splitter rejects dependency cycles.
268. Lock files excluded from splitter analysis.
269. Conflict markers: write @ours/@theirs/@base to resolve.
270. conflict://* bulk resolution.
271. Config importer: read rules from other agents' formats.
272. Importer maps to xavani skill format with provenance comment.
273. pr:// read scheme: read a PR as a path.
274. issue:// read scheme.
275. skill:// read scheme.
276. RPC mode: NDJSON over stdio for embedders.
277. RPC UI frames: tool cards as answerable requests.
278. Session branching: fork a session at any turn.
279. Branch isolation proven in turn-bank state (0.1.2 lineage).
280-330. Tests per port (50 items), each port verified against bench before merge.

## W6 — Overlooked-feature pack (items 331-390)

331. Session handoff file writer: SESSION_HANDOFF.md generator.
332. /handoff command with scope flags.
333. Finish notification: alert on long-task completion via gateway.
334. Notification channels: session, telegram, discord.
335. Deterministic macro slash commands: /macro define/run.
336. Macros stored in ~/.xavani/macros/.
337. Diff-review card before every multi-file apply.
338. /diff command: show pending changes.
339. /apply and /reject commands for staged changes.
340. Session forking UI: /fork at turn N.
341. Session list with titles and costs.
342. Scheduled self-contained jobs: cron prompt validation at create time.
343. Team shared config: repo-level xavani.config.yaml merge order.
344. Cost dashboard: per-session, per-day, per-model table.
345. Skill auto-load triggers: frontmatter condition field.
346. Trigger evaluation logged for audit.
347. Multi-platform delivery hardening: retry and dedupe.
348. /export command: transcript to markdown with metadata.
349. /import command: resume from exported file.
350. Clipboard integration: copy last code block.
351-390. Tests and docs per feature (40 items).

## W7 — CLI and TUI polish (items 391-430)

391. Shell completions generated from live COMMAND_REGISTRY metadata.
392. Completion scripts for bash, zsh, fish.
393. Model name completion against model catalog.
394. Session resume completion against on-disk sessions.
395. Two new skins beyond xavani-darkblue.
396. Skin validation on load with clear errors.
397. Banner refresh with version and mode line.
398. Tips rotation: contextual, not random.
399. Doctor expansion: check permissions file, loops dir, bench results dir.
400. Error message quality pass: every user-facing error names the fix.
401-430. Per-command help text audit (30 items).

## W8 — Personal, business, and company workflow packs (items 431-470)

431. Inbox triage pack: prioritize, draft, send-verify skills.
432. Meeting notes pack: transcript to decisions, owners, tickets.
433. Invoice extraction pack (extends bench task).
434. Weekly review pack: commitments, stalled work, next-week plan.
435. Research monitor pack: recurring digest with dedupe.
436. Document draft pack: briefs, memos, letters.
437. Expense log pack: capture, categorize, export CSV.
438. CRM-lite pack: contact notes and follow-up reminders.
439. Each pack ships with eval tasks in the bench suite.
440. Pack index page on the website.
441-470. Per-pack tests and docs (30 items).

## W9 — Docs, site, release engineering (items 471-500)

471. CHANGELOG 0.2.0 entry in Keep-a-Changelog format.
472. Version bump to 0.2.0 in pyproject.toml plus uv.lock regen.
473. Migration notes from 0.1.x.
474. README feature section rewrite.
475. Website docs: permission modes page.
476. Website docs: loops page.
477. Website docs: eval harness page.
478. Website docs: model roles page.
479. Install script check for Python 3.11+ and new data dirs.
480. Windows install.ps1 parity check.
481-500. Screenshots, changelog polish, release checklist, tagged release notes (20 items).

## Execution order

Phase 1: items 1-30 (W1). Phase 2: 181-240 (W4). Phase 3: 31-100 (W2).
Phase 4: 101-180 (W3). Phase 5: 241-390 (W5+W6). Phase 6: 391-500 (W7-W9).
Serial children only. Bench gate at each phase end.
