name: browser-browser-use
version: 1.0.0
description: "Browser Use (https://browser-use.com) cloud browser backend. Supports both direct BROWSER_USE_API_KEY and the managed Xavani Portal tool gateway. Also powers the 'Xavani Portal subscription' UX flow that bills usage to a portal subscription."
author: Enternovate
kind: backend
provides_browser_providers:
  - browser-use
